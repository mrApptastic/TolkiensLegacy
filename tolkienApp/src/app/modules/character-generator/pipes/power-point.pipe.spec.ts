import { PowerPointPipe } from './power-point.pipe';

describe('PowerPointPipe', () => {
  it('create an instance', () => {
    const pipe = new PowerPointPipe();
    expect(pipe).toBeTruthy();
  });
});
